.page-header
  = link_to producers_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_producer_path(@producer), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show producer

%dl.dl-horizontal
  %dt Name:
  %dd= @producer.name
