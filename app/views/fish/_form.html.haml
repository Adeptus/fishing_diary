= simple_form_for @fish do |f|
  = f.input :name
  = f.input :description
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), fish_index_path, :class => 'btn btn-default'
